<table class="mt-20">
    <tr>
        <th>หมายเลขคำสั่งซื้อ</th>
        <th>เวลา (dd/mm/yyyy)</th>
        <th>ยอดซื้อสินค้ารวม (บาท)</th>
        <th>เพิ่มโดย</th>
    </tr>
    <%
    sumtotal = 0
    orders.forEach((row) => {
        sumtotal += parseInt(row.total)
    %>
    <tr>
        <td><%=row.order_id%></td>
        <td><%=row.time.toLocaleDateString()%> <%=row.time.toLocaleTimeString("en-US", { hour12: false }).replace(/AM|PM/,'')%></td>
        <td><%=row.total.toLocaleString()%></td>
        <td><%=row.username%></td>
    </tr>
    <%                
    })
    %>
    <h3>รวมเป็นเงินเท่ากับ: <%=sumtotal.toLocaleString()%> บาท</h3>
</table>